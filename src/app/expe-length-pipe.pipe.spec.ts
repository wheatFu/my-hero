import { ExpeLengthPipePipe } from './expe-length-pipe.pipe';

describe('ExpeLengthPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ExpeLengthPipePipe();
    expect(pipe).toBeTruthy();
  });
});
